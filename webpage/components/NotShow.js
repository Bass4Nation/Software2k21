const NotShow = () => {
  return (
    <>
      {/* Denne siden blir vist om man går til en siden som man må være innlogget i. I en ikke innlogget state på siden */}
      <h2>Du må være innlogget for å se denne siden</h2>
    </>
  );
};

export default NotShow;
